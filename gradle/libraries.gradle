ext.google = [
    guava : "com.google.guava:guava:${guavaVersion}",
    guice : [
        core      : "com.google.inject:guice:${guiceVersion}",
        servlet   : "com.google.inject.extensions:guice-servlet:${guiceVersion}",
        persist   : "com.google.inject.extensions:guice-persist:${guiceVersion}",
        assisted  : "com.google.inject.extensions:guice-assistedinject:${guiceVersion}",
        multibind : "com.google.inject.extensions:guice-multibindings:${guiceVersion}"
    ]
]

ext.javax = [
	api : [
		servlet : "javax.servlet:javax.servlet-api:3.0.1",
		rs      : "javax.ws.rs:javax.ws.rs-api:2.0.1"
	]
]

ext.logs = [
	slf4j  : "org.slf4j:slf4j-api:${sl4fjVersion}",
	simple : "org.slf4j:slf4j-simple:${sl4fjVersion}"
]

ext.data = [
   h2: "com.h2database:h2:1.4.193"
]

ext.governator = [
	core  : "com.netflix.governator:governator-core:${governatorVersion}",
	jetty : "com.netflix.governator:governator-jetty:${governatorVersion}",
	test  : "com.netflix.governator:governator-test-junit:${governatorVersion}"
]

ext.philips = [
	boot       : "com.philips.app.tools:philips-boot:${philipsApplicationVersion}",
	commons    : "com.philips.app.tools:philips-commons:${philipsApplicationVersion}",
	session    : "com.philips.app.tools:philips-session:${philipsApplicationVersion}",
	data       : "com.philips.app.tools:philips-data:${philipsApplicationVersion}",
	resteasy   : [
		core      : "com.philips.app.tools:philips-resteasy:${philipsApplicationVersion}",
		multipart : "com.philips.app.tools:philips-resteasy-multipart:${philipsApplicationVersion}"
	]
]
